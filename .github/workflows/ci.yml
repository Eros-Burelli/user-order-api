name: CI
concurrency:
    group: ci-${{ github.ref }}
    cancel-in-progress: true

on:
  push:
    branches: ["main", "master"]
  pull_request:

jobs:
  test:
    runs-on: ubuntu-latest
    timeout-minutes: 10

    services:
      mysql:
        image: mysql:8.4
        env:
          MYSQL_DATABASE: userorderdb
          MYSQL_USER: userorder
          MYSQL_PASSWORD: ${{ secrets.CI_DB_PASSWORD}}
          MYSQL_ROOT_PASSWORD: ${{ secrets.CI_DB_ROOT_PASSWORD}}
        ports:
          - 3306
        options: --health-cmd="mysqladmin ping -h 127.0.0.1 -uuserorder -p${MYSQL_PASSWORD}" --health-interval=10s --health-timeout=5s --health-retries=10

    env:
      SPRING_DATASOURCE_URL: jdbc:mysql://127.0.0.1:3306/userorderdb
      SPRING_DATASOURCE_USERNAME: userorder
      SPRING_DATASOURCE_PASSWORD: ${{ secrets.CI_DB_PASSWORD}}
      JWT_SECRET: ${{ secrets.CI_JWT_SECRET }}
      JWT_EXPIRATION: 3600000

    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Set up Java
        uses: actions/setup-java@v4
        with:
          distribution: temurin
          java-version: '17'
          cache: maven

      - name: Run tests
        run: mvn -B clean verify